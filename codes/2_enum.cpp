// Enum a user defined data type which holds a collection of constant variables each having a predefined or specified value

#include <bits/stdc++.h>
using namespace std;

enum Level{
    LOW = 25,
    MEDIUM = 50,
    HIGH = 75
};

int main()
{
    Level l = LOW;
    cout << l << '\n';
}